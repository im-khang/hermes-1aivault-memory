# 1AIVault Memory installed

Enable provider and verify:

```bash
hermes config set memory.provider 1aivault-memory
hermes memory status
hermes mcp test 1aivault
```

Restart gateway after activation:

```bash
hermes gateway restart
```
